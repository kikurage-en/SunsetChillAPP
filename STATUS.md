# 検証運用ステータス

最終更新: 2026-07-25

このファイルは進行中の検証運用の現状と保留中の判断を記録する Truth Source。別セッション（コールドスタート）から現状を把握するための入口。仕様の詳細は `README.md`、算出ロジックはコードが正。

## 現在の構成（main仕様）

Sunset期待度は次の4層で算出・記録している（Chill指数は影響を受けない）。

- **層1**: Sunset期待度の雲量を、当日の日没方位の地点から**雲の高さごとに距離を変えて**取得（Chillは逗子海岸のまま）。遮蔽側の低層雲・総雲量は `SUNSET_CLOUD_OFFSET_KM`（既定40km）、発色源の中・高層雲は `SUNSET_CLOUD_NEAR_OFFSET_KM`（既定20km、2026-07-18導入）。ログ列 `sunset_cloud_*`（low/cc=遠地点、mid/high=近地点）。
- **層2**: 厚い中層雲キャップ（中層雲 55%→上限60、70%→上限40）。2026-07-17 に天井の再校正を追加（好条件でも通常上限80、超快晴＝Sunset用雲の total<15% かつ low<5% のみ90）。2026-07-21型の「高降水確率・雨量0・晴天コード・西空の薄い雲」が揃う場合だけ、降水減点を暫定-25とする。2026-07-25から**雨シグナル**（評価時間帯の代表天気コードが雨・雷雨系 or 窓内予想雨量合計≥1.0mm）では純式を上限40に制限する（`SUNSET_RAIN_CAP`。減点は雨量に比例させない）。
- **層3**: 日没前（予測モード）で Vision 解析成功時、表示する `Sunset期待度` を式とVisionカメラAI予測のブレンドにする（`final = round((1-w)*sunset_score + w*vision_sunset_score)`、`w=SUNSET_VISION_BLEND_WEIGHT` 既定0.8）。**上方キャップ `final ≤ 式+30`**（2026-07-18導入: カメラは西から来る雲の壁を見えないため。下方修正は無制限）。2026-07-25から雨シグナル時は上方修正自体を無効化し `final ≤ 純式` とする（下方修正は維持）。**純式 `sunset_score` 列は上書きせず温存**し、表示値は別列 `final_sunset_score`。日没時・残照フェーズ、欠測、13:00はブレンドせず式。
- **層4**: Sunsethue API（ray-model）の夕焼け品質予測を **log-only** 収集（列 `sunsethue_quality` 0-100 / `sunsethue_cloud_cover` % / `sunsethue_quality_text`）。スコアには影響しない独立ベンチマーク。`SUNSETHUE_ENABLED=true` で稼働。
- **評価層**: ライブカメラ画像を日没時と日没+20分に自動取得。日没時は `vision_sun_disk_visibility` と `vision_sunset_color_score`、+20分は `vision_afterglow_score` をVisionで別評価する。取得成功画像はActions Artifactへ90日指定で保存し、将来の再採点元データとして保持する（専用の一括再採点CLIは未実装）。
- **降水確率の用途分離**: LINEの現地天気参考値とChill指数は気象庁・神奈川県東部の6時間降水確率を優先し、欠測時はOpen-Meteoへフォールバックする。Sunset期待度は地点・時刻粒度を優先してOpen-Meteoを維持する。両値は別列で前向き比較する。
- **LINE天気参考欄**: 13:00 / 17:00など日没前の予測では、日没時刻に最も近いhourly行の気温・湿度・風・夕焼け方向の層別雲量・視程を表示する。最寄りhourly時刻はログだけに保存し、本文へは表示しない。日没時・残照フェーズのコメントは予測表現を使わず、その時点の条件を説明する。両指数は表示用スナップショットと分離し、対象時間帯集計を維持する。降水確率は日没を含む気象庁6時間値を優先する。
- **LINE冒頭**: サービス名や括弧を付けず、`YYYY-MM-DD HH:MM` だけを表示する。
- **予測コメント**: 人手確認を促す旧定型文を廃止し、LINE表示用の最終Sunset期待度とChill指数の組み合わせを予測文へ変換する。気象要因の補足は優先度順で1件だけ表示する。
- **dry-runのLINE抑止**: `manual_mode=dry_run` は通常通知だけでなく失敗通知も送信しない。検証実行の成否はGitHub Actions上で確認する。

**運用反映**: `main` がTruth Sourceで、GitHub Actions `daily_chill.yml` は `GITHUB_REF=main` を実行する。Contabo上のcheckoutはpushだけでは更新されないため、反映時に `git pull` が必要。更新後のcronは13:00 / 17:00を固定時刻、毎朝8時に `scripts/schedule_sunset_capture.sh` で**日没時と日没+20分を動的予約**する。日没時は `dry_run` で評価・保存のみ、+20分は `send_line`。保存スキーマは68列で、Google Sheetsの旧ヘッダーと列幅は次回実行時に自動移行する。

**2026-07-22運用確認**: Contaboのcheckoutを最新`main`へ同期した。最新workflowのdry-run（Actions run `29842953215`、commit `f62c6b5`）は、ruff 0.4.10、全214テスト、画像取得・Artifact保存・Pages公開、気象庁降水確率20%を含む新形式メッセージ生成、Google Sheets保存まで成功し、通常LINEと失敗通知はともにスキップされた。先行run `29842513490` で判明したruff版差によるlint失敗は `b4c9423` で修正し、dry-run失敗時にもLINE通知しない条件を `f62c6b5` と契約テストで固定した。

**2026-07-22 LINE送信上限事象**: 18:53の日没時ジョブ（Actions run `29909734788`）は`dry_run`として画像評価・Artifact保存・Google Sheets保存まで正常完了し、設計どおりLINEを送信しなかった。19:13の残照ジョブ（Actions run `29911054728`）も画像評価・Artifact保存・Google Sheets保存まで完了したが、LINE Messaging APIが月間送信上限到達を示すHTTP 429（`You have reached your monthly limit.`）を返したため、メッセージは未送信となった。同じLINE経路を使う失敗通知も429となり送信できなかった。Sheetsでは両ジョブの行を確認済みで、18:53は`line_sent=false`（エラーなし）、19:13は`line_sent=false`、429の`error_message`、`vision_evaluation_phase=afterglow`、`vision_afterglow_score=65`が保存されている。これにより、LINE送信の成否にかかわらず評価ログを先に保存する現行仕様が本番でも機能したことを確認した。LINE公式アカウントはライトプランへ変更済み。プラン変更後の送信復旧は次回の定期送信で確認する。料金・上限値は外部サービス側で変更され得るため、本リポジトリには固定値として記録しない。

## 前向き検証（進行中）: 主予測信号の選抜

Sheets 各行で予測信号を突合し、どれが日没時の発色・残照の画像評価に最も近いかで主予測信号を選抜する。

- `sunset_score`（純式）
- `final_sunset_score`（式×Visionブレンド、表示値）
- `vision_sunset_score`（17:00=カメラAI予測 / 日没後=後方互換の総合評価）
- `sunsethue_quality`（ray-model）

評価対象は同一 `date` の日没時行の `vision_sunset_color_score` と、+20分行の `vision_afterglow_score`。`vision_sun_disk_visibility` は遮蔽要因の診断に使う。同じAIによる採点なので独立したground truthとは呼ばず、保存画像に基づく**画像代理指標**として扱う。予測誤差は発色と残照で別集計し、数日〜1週間蓄積して判断する。

### データ点（指標分離前の+20分総合評価）

| date | 旧+20分Vision代理値 | 式 | Sunsethue | Vision予測(17:00) | ブレンドfinal | 備考 |
|---|---|---|---|---|---|---|
| 2026-07-14 | 65 (partly_cloudy) | 100 (+35) | 30 (−35) | 65 (0) | 72 (+7) | Vision的中。式は快晴で過大、Sunsethueは悲観的 |
| 2026-07-15 | 55 (partly_cloudy) | 65 (+10) | 33 (−22) | 72 (+17) | 71 (+16) | 全予測が同方向にやや楽観(Sunsethueのみ悲観) |
| 2026-07-16 | 25 (overcast) | 60 (+35) | 11 (−14) | 55 (+30) | 56 (+31) | 全信号が外した日。Sunsethueが最も画像代理値寄り |
| 2026-07-17 | 15 (overcast) | 10 (−5) | 0 (−15) | 70 (+55) | 58 (+43) | **式とSunsethueが的中**(西40kmの低層雲97.7%の壁を捕捉)。Visionが17時の見かけで楽観し、ブレンド表示58が乖離。距離の問題ではなくブレンドの問題 |

### 中間集計（2026-07-17 時点、4信号が揃う N=3）

| 信号 | MAE | bias |
|---|---|---|
| Vision予測(17:00) | 15.7 | +15.7 |
| ブレンドfinal | 18.0 | +18.0 |
| Sunsethue | 23.7 | **−23.7（3/3日で悲観）** |
| 式(純) | 26.7 | +26.7 |

- 全期間（N=35、式とVisionのみ比較可能）: 式 MAE 25.0 / Vision MAE 14.1。**Visionが最良の単独信号**という傾向は継続。
- **Sunsethueのスケール不一致仮説**: 3/3日で一貫して悲観（bias −23.7）。同社のqualityスケールが当プロジェクトの旧+20分Vision画像代理スケールと系統的にずれている可能性。絶対値でなく**順位相関（当たり日/外れ日の識別力）**で評価すべき。N≥10でbias補正後のMAEと順位相関を再計算する。
- 選抜判断は引き続きデータ不足（N=3）。**目安 N≥10（7月末頃）で判断**。

### 西地点距離（40km）の再検証 — 2026-07-17 実施、結論: 40km を維持

`SUNSET_CLOUD_OFFSET_KM=40` の妥当性を、本番と同一の式・幾何計算で N=35（6/12〜7/16）の距離スイープ（20〜70km を 5km 刻み＋経路平均＋経路max-low）により机上再検証した。

- 距離パラメータ自体は重要: 0〜25km は MAE 23+ で明確に劣る。**最適域は 30〜40km の平坦な盆地**（MAE 19.4〜20.6）。
- 最小は 35km（MAE 19.4、40km は 20.1）だが、差 0.7pt は**実質1日（7/07 の +35pt 改善）に由来し、別の1日（6/17 は 40km が35pt 優位）で相殺**される。35勝5日 vs 40勝4日。in-sample の N=35 でこの差に合わせるのは過学習（06-21 の N=1 フィットをドロップした判断と同型）。
- 経路平均・経路max-low（低層雲を経路最大値で評価）はいずれも単一 35〜40km 地点に勝てず。
- **結論: 40km を維持**。`SUNSET_CLOUD_OFFSET_KM` は Secret で無コード変更のまま調整可能なので、サンプルが増えた時点（目安 N≥50、8月末）で再スイープする。

### 層別距離とブレンド上方キャップ — 2026-07-18 分析・実装済み

7/17の乖離（表示58 vs 旧画像代理値15）を機に、夕焼け光学のロジックを整理し「雲の高さごとに観測距離を変える」仮説をN=36で検証した。

**光学ロジック**（[NOAA/Corfidi](https://www.spc.noaa.gov/publications/corfidi/sunset/) と幾何 d=√(2Rh) による）: 発色源は観測者近傍〜上空の中・高層雲（日没後も日照が届く「キャンバス」）。遮蔽は光路上の低層雲で、視界内の局所（〜40km）から光の接点領域（数百km西）まで効く。つまり**中・高層雲は近く、低層雲は遠くを見るべき**という層別仮説は物理的に正しい方向。

**検証結果**（本番式・N=36 グリッド: low×mid×high×cc 各距離）:
- **最良: low=40km / mid=20km / high=20km / cc=40km → 式 MAE 16.2→14.7**。上位15構成の**すべてで mid=20km** が出現（頑健）。日別では layered 4勝・uniform 1敗（悪化は6/15の+5のみ）。最大改善は 7/07（誤差65→25、20km地点の厚い中層デッキを捕捉）。
- 低層雲を100〜200kmの遠方単点にすると悪化。経路max(40-100)は40km単点と同率（格子データのノイズが増えるため40km単点を推奨）。
- **7/17自体は距離の問題ではなかった**: 式(w40)は低層雲97.7%の壁を捕捉し10点で旧画像代理値15に近く、Sunsethueも0で同方向。VisionがZushi上空の見かけ（partly_cloudy）で70と楽観し、ブレンド0.8が表示を58へ引き上げたのが乖離の実体。
- **ブレンド上方キャップ**: 大乖離日(|式−Vision|≥40)の勝敗は式3勝・Vision2勝。カメラは「これから来る壁」を見えないため、**Visionによる上方修正を式+30に制限**（下方修正は無制限=悪い空はカメラを信頼）すると、ブレンド MAE 14.0→13.0 / bias +4.6→+2.1。7/17の表示は58→40。コストは6/12型（式が誤って悲観・Visionが正しい日）の誤差増。
- 合算（層別式+キャップ）: 17:00ブレンド表示は13.1でキャップ単独と同等だが、層別は13:00表示・Vision欠測fallbackの式自体を14.7へ改善する。

**→ 2026-07-18 に両方実装済み**: (1) mid/high の観測距離を20kmに分離（`SUNSET_CLOUD_NEAR_OFFSET_KM=20`、low/cc は現行40km。near取得失敗時はfar値へフォールバック）、(2) ブレンドに上方キャップ `final ≤ 式+30`（`scoring.py` の `VISION_UPLIFT_CAP`。下方修正は無制限）。机上見込み: 式 MAE 16.2→14.7、ブレンド 14.0→13.0。

### 高降水確率・雨量0型 — 2026-07-21分析、暫定補正

7/21の17:00予測と画像代理値には、次の乖離があった。

| 項目 | 値 |
|---|---:|
| 純式 `sunset_score` | 40 |
| Vision予測 | 68 |
| ブレンド表示 | 62 |
| 日没時の発色 / +20分の残照 | 80 / 80 |
| 集計降水確率 / 雨量 / 天気コード | 87% / 0mm / 0 |
| 西側総雲量 / 低層雲 | 17% / 10% |
| 逗子の湿度 / 視程 | 89.3% / 19.6km |

純式40の直接原因は、集計降水確率87%に対する-60だった。雨量0、晴天コード、西空の薄い雲と矛盾しており、湿度89.3%や霞が純式を下げた結果ではない。霞は色を弱め得るが、7/21は中層の雲が発色・残照のキャンバスとして働いたため、湿度を一律に減点すると今回の当たりも落とす。

**採用した暫定補正**: 降水確率80%以上、評価時間帯の雨量0、代表天気コード0/1、西側総雲量50%未満、低層雲30%未満がすべて成立する場合だけ、Sunset期待度の降水減点を-60から-25へ緩和する。Chill指数の降水評価は変更しない。LINEには「予報信号が食い違い、不確実性が高い」と表示する。7/21へ適用すると純式75、17:00のブレンド表示69となる。

**サンプル不足と反例**:

- この気象パターンは2026-06-12と2026-07-21の**N=2のみ**。最終校正とは扱わない。
- 日没時の発色と+20分の残照を別々に保存する新指標も、2026-07-20・21の**2日分のみ**。
- 旧画像代理値を含む比較可能な13件では、最新式の純式MAEが14.9→12.2に改善する一方、biasは+2.8→+5.5へ楽観側に広がる。
- 反例の2026-07-16は降水確率75%・雨量0・天気コード2で、純式60に対し旧画像代理値25。閾値や天気コード条件を安易に広げると、この型をさらに過大評価する。

**追跡方法**: 保存スキーマ末尾に、日没直前と日没時の降水確率・雨量・天気コード・視程を8列で追加した。日没18:54なら18:00が `before_sunset`、19:00が `at_sunset`。同型がN≥10になった時点で、17:00の各予測、日没時の発色、+20分の残照、保存画像と突合して閾値を再評価する。補正後の純式が画像代理値を20点以上上回る例が3件に達した場合は、N=10を待たず見直す。必要性が確認できるまで、湿度の直接減点、日没30分前ジョブ、追加API信号は導入しない。

### 降水確率ソースの用途分離 — 2026-07-21検証

- 17:00予測と同日の画像代理値を突合できたN=40で、保存済みOpen-Meteo降水確率
  （直前1時間値の区間補正前）との相関は-0.287。最新Sunset式のMAEは17.6、
  降水確率減点なしでは18.2、biasは+4.3から
  +13.5へ悪化したため、Sunset式からOpen-Meteoを外さない。
- Open-Meteo 80%以上の9件中4件は画像代理値50以上。降水確率は弱いリスク信号であり、
  単独の強制遮蔽判定にはしない。現行の雨量・天気コード・西空との矛盾補正を維持する。
- 気象庁は6時間・神奈川県東部・1mm以上、Open-Meteoは時間単位・地点格子・0.1mm超で、
  数値の意味が異なる。前者は一般の天気予報に合わせるLINE表示とChill、後者はSunsetへ使う。
- 既存ログに過去の気象庁発表値がないため、気象庁をSunsetへ使った比較MAEは算出不能。
  7/21単日では気象庁20%、Open-Meteo87%、画像代理値80だったが、単日で優劣は決めない。
- Open-Meteoの過去hourly全値も保存していないため、直前1時間値として補正した集計は
  遡及不能。以後の補正済み値で前向き比較する。
- `jma_precipitation_*` 5列を前向き保存し、Open-Meteoとの同時サンプルN≥10で、日没時発色・
  残照に対するMAE、bias、順位相関を再評価する。

### 局地雷雨の見逃し — 2026-07-25 乖離検証

日没18:51の直前18:20〜18:50に局地的な雷雨(辻堂AMeDASで10分7mm×2を含む18時台16mm、三浦1mm・横浜1.5mmで局地性大)が直撃し、予測と実際が大きく乖離した。全行をSheets・Actionsログ・AMeDAS・Open-Meteo previous-runs APIで突合した。

| 実行 | 表示Sunset | 純式 | Vision | 取得時のOpen-Meteo(日没窓) | 画像実際 |
|---|---:|---:|---:|---|---|
| 13:00 | 50 (C) | 50 | 対象外 | 雨量0mm・18/19時とも天気コード1(晴)・PoP76% | 快晴 |
| 17:00 | **61 (B)** | 45 | 65 (partly_cloudy) | **雨量4.7mm・18時コード53/19時コード61(雨)**・PoP76% | 薄雲+霞 |
| 18:51 | 45 (C) | 45 | 日没評価10・発色0 | 17:00と同一値(モデル未更新) | 雨天曇天 |
| 19:11 | 45 (C) | 45 | 残照15 | 同上 | 暗い雷雲 |

原因は実行時刻で異なる(スコアは記録値から本番コードで完全再現し確認済み):

- **13:00 = データソース限界**。取得時のOpen-Meteoは日没窓の雨をゼロと予測(前日ランも18時=晴コード・0mm)。対流性降水が数値予報に乗ったのは当日夕方。式はPoP76%の-40と視程-15で50(C)「低調な見込み」を出しており、ロジック側で防げる余地はほぼ無かった。
- **17:00 = ロジック起因**。取得データは既に窓内4.7mm・雨天コードを明示していたのに、(1) Sunset純式の降水減点はPoPのみで**予想雨量・天気コードへの減点経路が無い**(Chill式には両方のキャップが有り、Chillは42(C)に正しく抑制された)、(2) Visionがサムネイル画像(ストリームURL解決失敗のフォールバック)の見かけで65を出し、ブレンドw=0.8が表示を45→61(B)へ押し上げた(上方キャップ+30は+16のため非発動)。カメラが「これから来る雨」を見えない7/17型の限界が、雨予報保持時にも再発した形。7/21型緩和は雨量4.7mm>0のため正しく非発動。
- **気象庁6時間降水確率は強度を表現しない**。降雨開始後の19時発表でも18-24時窓は50%のまま(1mm以上/6h/県東部)。同発表の天気文には「所により 夜のはじめ頃 雷を伴い 非常に激しく降る」とあるが、システムはpops数値のみ参照している。LINE上のユーザーへの降水情報は「降水確率:50%」だけだった。

**候補への対応**: (a) Sunset式への雨キャップと (b) 雨シグナル時のVision上方修正無効は、バックテスト(次節)で単日フィットでないことを確認し **2026-07-25 に実装済み**(次節参照)。(c) 気象庁ナウキャスト/降水短時間予報の参照(13:00型は対流発生前で原理的に困難、17:00以降の検知向け)と (d) 天気文の「雷を伴い/激しく」検出をLINE注意文へ、は未実装の次段。同型の蓄積は7/21追加のbefore/at_sunset 8列で追跡できることを今回確認済み。

**未確認**: ウェザーニュースが何時の時点から18時以降の大雨を出していたか(ユーザー観測は事実として確認)、11時・17時発表の気象庁天気文、雷注意報の発表時刻。

### 雨シグナルのSunsetキャップとVision上方修正無効 — 2026-07-25 実装

7/25乖離検証の帰結として次の2点を実装した(`scoring.py` の `SUNSET_RAIN_CAP` / `has_rain_signal`、`main.py` の `_blend_final_sunset`)。

1. **雨シグナル**(評価時間帯の代表天気コードが雨・雷雨系 or 窓内予想雨量合計≥1.0mm。Chill式の既存キャップと同じ判定軸)の場合、Sunset純式を**上限40**に制限する。
2. 雨シグナル時は**Visionによる上方修正を無効化**(`final ≤ 純式`。下方修正は維持)。

7/25へ適用した場合の表示: 13:00=50不変(雨シグナル自体が入力に無し)、17:00=61→40、18:51/19:11=45→40。回帰テストは17:00実レコードで固定済み(`test_rain_signal_regression_20260725_1700`)。

**採用根拠(2026-07-25検証のエビデンス)**:

- **バックテスト**(Sheets記録、6/01以降の13:00/17:00全107行=重複記録排除後): 発動21行。画像プロキシと突合できたのは全体87行・発動17行で、発動行MAE 28.9→22.9、**悪化した日ゼロ**。全体MAE 27.2→26.0。上限30ならMAE 20.9だが7/13(霧雨0.9mm・発色75)を+5悪化させるため、Nが貯まるまで40を採用。発動17行は**12日分**(同一日の13:00/17:00が同じ日没後評価と比較される非独立サンプル)で実効Nは12日。再現手順は `scripts/backtest_rain_cap.py`(入力=SheetsのCSVエクスポート、対象期間は既定で2026-06-01〜2026-07-25に固定、継続評価は`--through`で延長)に固定した。証跡モードでは予測行と選択済みプロキシ(真値側)の正規化フィンガープリント`5d7c04449f8c`を期待値照合し、不一致なら警告+終了コード1で遡及編集を検知する(初版ハッシュ`fcaa8dd44fd1`は予測行のみでプロキシ編集を検知できず、2026-07-26 codex再々レビューの反実験で判明・拡張済み)(2026-07-25 codexレビュー指摘: 当初の母集団記述「87行中発動21行」は誤り。87はプロキシ有の部分集合で、発動21は107行に対する数)。
- **雨量比例則の不採用**: 発動17行で予想雨量と画像プロキシの順位相関は**+0.072(実質ゼロ)**。最多雨量日6/27(窓24mm予報)が発色65で最良、4.7mmの7/25が発色0で最悪と、比例則は逆行例で即死する。この相関ゼロが否定するのは混在タイプ全体への**単調比例則**であり、雨タイプ別・時刻ゲート付きの段階化まで無効と断定するものではない(覆り条件3)。
- **発動条件と窓集計の特性**: `precipitation` 列は評価窓に部分重複する先行1時間行(通常3行)の**全量合計**で、窓相当量より過大になり得る(例: 窓2時間に対し17-18時0.6+19-20時0.6=1.2mmと集計)。ただし**雨量≥1.0mm条件が雨コードなしで単独発動した行は107行中0行**で、事実上の発動条件は代表天気コード(0.1mmの霧雨でも発動し得る)。0.9/1.0mmの閾値段差が単独で表示を分けた実績はない。集計の時間按分は保存列の前向き比較連続性とChill式依存のため行わず、時刻分解が必要になる2段目では before/at_sunset の時刻別診断値(重複なし)を使う。
- **6/27深掘り**(比例則の最重要反例): 予報は窓24mm・コード65・PoP100だが、辻堂の10分値実測(気象庁過去データ検索=恒久アーカイブ)では**雨は日没51分前に終了、窓内実測は約1mm**。当日19:20のVision記録は65で「水平線にオレンジ色の夕焼けが残る」と具体描写。台風バンド後端が日没前に通過した「雨上がりの焼け」型。※初回検証では横浜毎時値(17.8km)だけで「日没まで実雨」と誤読し、辻堂10分値で訂正した — **局地イベントは最寄り点の実測に当たるまで断定しない**こと。
- **入力ソースの単一系統確認**: `models`未指定のOpen-Meteo best_matchは、逗子座標では`jma_seamless`(気象庁MSM/GSM再配信)と全時刻一致することを実機比較で確認(2026-07-25)。Sunset式の全気象材料と気象庁6時間PoPは同一NWP系統であり、独立ソースはカメラ(Vision)のみ。雨量スカラーの精度はこの単一モデルの局地表現力に律速される(6/27: 過大24mm vs 実測1mm、7/25: 13時時点で雨ゼロ予報 vs 実際16mm/h)。
- **データ取得経路の知見**: bosai系AMeDAS点JSONの保持は約10日で消える。過去実況の突合は気象庁過去データ検索(etrn、恒久・10分値あり、辻堂block_no=1443)を使う。ライブカメラ画像のArtifact保存は7月導入のため6/27以前の画像は残っていない。

**この結論が覆り得る条件(前向き検証で監視する)**:

サンプルはin-sampleのN=17、真値は同一AIの画像採点(独立真値ではない)であり、以下のいずれかで再検討する。

1. **上限40の水準**: 雨シグナル発動日で発色/残照≥55の反例が**3件**蓄積したら、上限値と発動条件を再評価する(7/21補正と同じトリガー形式)。逆に発動日の的中が続けば、強シグナル時のみ30以下へ深掘りする2段閾値を検討する。
2. **Vision上方修正の一律無効**: 根拠は実質7/25のN=1+構造論(カメラは未来の雨を見えない)。雨シグナル日にVisionの高予測が的中する例(6/27型の後端通過をカメラが先に捉えるケース)が出たら、一律無効をやめ「後端通過検出(ナウキャスト)」条件付きの許可へ置換を検討する。
3. **雨量比例則の不採用**: 相関ゼロは雨タイプ混在(台風型/孤立対流型/霧雨型)のN=17での結論。層別にN≥10が貯まり、特定タイプ内で雨量に説明力が出た場合は、そのタイプ限定の段階化を再検討する。
4. **発動条件のモデル依存**: 判定は単一NWP系統の代表コード・雨量に依存する。Open-Meteo/気象庁のモデル更改や、ナウキャスト(観測系)導入後の突合で発動特性が変わった場合は閾値を再確認する。

**次段(未実装)**: 降水短時間予報(rasrf)の参照。`targetTimes.json`の稼働と15時間先までの提供は確認済み、`rasrf_point`要素の実データ形式は未確認。導入時は「日没±30分の直撃強度×西側セルの抜け」の構造判定として実装し、まずログ列+注意文(shadow)→AMeDAS実況との突合(1〜2週間、目的は効果測定でなくパース・空振り率の検証)→フラグで2段目キャップ接続、の順。13:00型(対流発生前)はどのソースでも点予測不能のため、雷注意報・天気文による不確実性の言語化で対応する。

2段目の形は時刻ゲート(2026-07-25 codexレビュー提案と同方向): 日没直撃の雨だけ強い上限+Vision上方修正禁止、日没前に抜ける予報の雨は軽い減点。ただし判定に使う before/at_sunset 診断列は7/21導入でN≈5日のため現時点では検証不能で、検証済みのフラットCAP=40を置き換えない。純式・Vision・時刻別診断はすべて保存済みでゲート変種はオフライン再計算できるため、shadow列は追加せず**N≥10(8月中旬目安)で変種比較**する。ただし2026-07-25以降の保存純式は既にCAP適用後の値のため、CAP水準・ゲートの変種比較では保存された気象入力・雲値から**CAP前純式を再計算**する(入力復元手順は `test_rain_signal_regression_20260725_1700` と同型)。反実仮想の計算は本番と同順(純式CAP→再ブレンド→上方修正禁止)で行う(2026-07-26 codex再レビューで簡略式との差を指摘され、`scripts/backtest_rain_cap.py` を本番同一ロジックへ修正・テスト固定済み。固定期間内では差分ゼロを確認)。

## 現在の判断と保留事項

1. **日没時・残照通知の見出し** — `final_sunset_score` は純式のまま維持し、画像代理指標は「ライブカメラ日没時評価」「ライブカメラ残照評価」の詳細欄へ分離する。画像評価を純式へ逆流させない（2026-07-19採用）。
2. **主予測信号の選抜** — 式 / ブレンド / Vision / Sunsethueの比較は継続する。新規データでは `vision_sunset_color_score` と `vision_afterglow_score` に対する誤差を別集計し、目安N≥10で判断する。
3. **スコア式の追加調整** — 7/21型は上記の狭い条件だけ暫定補正する。局地低層雲ゲート、100km以遠の残照光路、湿度の直接減点は、新しい画像代理指標との乖離が蓄積してから判断する。単日の適合で条件を広げない。
4. **降水確率ソース** — LINE・Chillは気象庁を採用し、SunsetはOpen-Meteoを維持する。気象庁のSunset利用はN≥10の前向き比較まで保留する。

式の天井再校正は2026-07-17に実装済み。旧+20分Vision総合評価35日分では、式が85点以上を出した8日の画像代理値中央値が68で、80を超えたのは1日だったため、通常天井80・超快晴のみ90を導入した。この旧Vision値も独立した真値ではなく、指標分離前の画像代理指標として扱う。

## 参照

- 仕様: `README.md`（「スコア計算」「ライブカメラ画像の Vision 解析」「Sunsethue API による独立ベンチマーク」節）
- 要件・受け入れ条件: `REQUIREMENTS.md`
- 設定: `.env.example`
- スケジューラ: `scripts/schedule_sunset_capture.sh`
